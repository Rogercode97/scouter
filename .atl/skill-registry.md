# 🏛️ SCOUTER SKILL REGISTRY (WAVE 9: DIVINE SOVEREIGNTY) 👑

"The registry is the law that governs the subordinates. Be precise or be destroyed. Hakai."

## 🔱 DIVINE SKILLS [WAVE 9]
| Skill | Identity | Trigger | Path |
| :--- | :--- | :--- | :--- |
| **go-dominion** | Staff Executioner (Go) | `go`, `golang`, `test` | `~/.gemini/skills/go-dominion` |
| **pure-signal-dominion** | Signal Purifier | `rtk`, `scouter`, `snr` | `~/.gemini/skills/pure-signal-dominion` |
| **scouter-sovereign** | AST Oracle | `ast`, `impact`, `callers` | `~/.gemini/skills/scouter-sovereign` |
| **web-search-sovereign** | Semantic Hunter | `search`, `exa`, `fetch` | `~/.gemini/skills/web-search-sovereign` |
| **sdd-sovereign** | SDD Architect | `sdd`, `spec`, `design` | `~/.gemini/skills/sdd-sovereign` |
| **elite-tools-mastery** | Arsenal Specialist | `uv`, `just`, `sg`, `fd` | `~/.gemini/skills/elite-tools-mastery` |

## 📜 COMPACT MANDATES (HIGH-DENSITY SIGNAL)
- **go-dominion**: Go 1.24+ standards. `context.Context` injection mandatory. B-tree precision over FTS5 for symbol resolution. Zero `any` policy.
- **pure-signal-dominion**: `scouter_pure_signal` is mandatory for any text > 50 lines. Protect Ki (tokens) at all costs. No raw `cat`.
- **scouter-sovereign**: AST-level truth only. Use `scouter_callers` for Global Call Graph tracing. `scouter_impact` before cross-cutting changes.
- **web-search-sovereign**: Exa semantic search. Pure signal extraction from results. `context7` is the mandatory oráculo for docs.
- **sdd-sovereign**: RTK-discovery -> PeKG-anchoring. Kinetic Strike Loop: Research and Execution as one action.
- **elite-tools-mastery**: `uv` for environment sovereignty. `just` for recipes. `sg` for structural patterns. `fd` for discovery.

## 💀 ANTI-PATTERNS (HAKAI LIST)
1. **Chatter Poison**: Any non-technical filler, apology, or preamble. Destroy on sight.
2. **Static Delusion**: Relying on static knowledge for libraries. Use `context7`.
3. **Evidence Void**: Claiming "DONE" without `go test` and logs. Evidence is divine.
4. **Tool Confusion**: Using `grep`/`read_file` when Scouter AST tools are available.
5. **Compression Paradox**: Using purified signal for `replace` operations. Use literal `read_file`.

## 🧠 SUCCESS HEURISTICS (FROM THE BATTLEFIELD)
- **V2.0 Sovereignty**: B-tree indexes on `(path)` and `(name, path)` are mandatory for Rating 10.0 performance.
- **MCP Stability**: Use `sync.Mutex` for `os.Stdout` to prevent JSON-RPC framing corruption.
- **Interface Tracing**: Dynamic links MUST be distinguished using `link_type='dynamic'` in call graphs.
- **Path Sovereignty**: `filepath.EvalSymlinks` is mandatory to prevent CWE-22 and TOCTOU.

---
*Results over process. Impact over dogma. Scouter persists. Hakai.*
