version: "2"
sql:
  - engine: "sqlite"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "store"
        out: "."
        sql_package: "database/sql"
        emit_json_tags: true
        overrides:
          - db_type: INTEGER
            go_type: int
