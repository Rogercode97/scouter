id: no-panic
language: go
rule:
  pattern: panic($$$)
message: "Unsafe Operation: Use of panic() detected. Use error return values instead."
severity: ERROR
note: "Production code should handle errors gracefully. Reserve panic() only for unrecoverable system states during initialization."
