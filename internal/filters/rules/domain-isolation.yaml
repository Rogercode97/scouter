id: domain-isolation
language: go
rule:
  pattern: import "database/sql"
message: "Hexagonal Violation: Domain layer should not import 'database/sql'. Use interfaces and repository patterns."
severity: error
note: "The domain layer must remain agnostic of the persistence implementation. Inject repositories via interfaces instead."
